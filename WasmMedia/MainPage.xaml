<Page x:Class="WasmMedia.MainPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:local="using:WasmMedia"
      xmlns:utu="using:Uno.Toolkit.UI"
      Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
  <ScrollViewer>
    <StackPanel utu:SafeArea.Insets="VisibleBounds"
          HorizontalAlignment="Center"
          VerticalAlignment="Center">
        <Button x:Name="myButton" Click="myButton_Click">LOAD FONT ASSET</Button>
        <TextBlock x:Name="tb1" >The quick brown fox jumped over the lazy dog.</TextBlock>
        <Button x:Name="chime1" Click="chime1_Click">PLAY ALARM.MP4 ASSET</Button>
    </StackPanel>
  </ScrollViewer>
</Page>
